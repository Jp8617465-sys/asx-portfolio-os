# ASX Portfolio OS - Frontend Environment Variables
# Copy this file to .env.local and fill in the values

# Backend API Configuration
# The base URL for the backend API (Render deployment)
# IMPORTANT: Do NOT include /api/v1 suffix - routes are served at root level
NEXT_PUBLIC_API_URL=https://asx-portfolio-os.onrender.com

# API Key for authenticating requests to the backend
# CRITICAL: This is server-side only (NOT prefixed with NEXT_PUBLIC_)
# Must match the OS_API_KEY value in your Render backend environment
OS_API_KEY=your_api_key_here

# Optional: Analytics (Phase 3)
# NEXT_PUBLIC_GA_ID=your_google_analytics_id

# Optional: Sentry Error Tracking (Phase 3)
# NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn
