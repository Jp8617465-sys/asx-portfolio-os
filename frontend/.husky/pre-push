#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🚀 Running pre-push checks..."
echo ""

cd frontend

# 1. Type check
echo "📝 TypeScript type checking..."
npm run type-check || {
  echo "❌ Type check failed."
  exit 1
}

# 2. Lint
echo "🧹 ESLint checking..."
npm run lint || {
  echo "❌ Lint failed."
  exit 1
}

# Note: Full test suite and build checks disabled temporarily
# due to pre-existing issues (localStorage SSR errors, low coverage)
# TODO: Re-enable after fixing these issues in a separate PR

echo ""
echo "✅ Pre-push checks passed!"
echo "✅ Safe to push to remote"
